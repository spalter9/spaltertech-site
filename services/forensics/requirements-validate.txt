# Minimal environment to run validate_measurements.py without torch/demucs.
# measurements.py only imports numpy/scipy/librosa — separation.py is the
# only file in this service that needs the heavy GPU stack, and this script
# never imports it.
#
#   python3 -m venv .venv-validate
#   .venv-validate/bin/pip install -r requirements-validate.txt
#   .venv-validate/bin/python validate_measurements.py
numpy==1.26.4
scipy==1.14.1
librosa==0.10.2.post1
soundfile==0.12.1
