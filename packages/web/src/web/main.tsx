// Entry point referenced by index.html — composition only, real bootstrap
// lives in __main.tsx (template-managed). Managed sign-in redirect completion
// runs on mount inside app.tsx.
import "./__main";
